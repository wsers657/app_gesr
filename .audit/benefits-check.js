(() => { applyLanguage('fr'); document.querySelectorAll('.route-active .reveal').forEach(el=>{el.classList.add('visible');el.style.opacity='1';el.style.transform='none';}); const intro=document.querySelector('.features-intro'); window.scrollTo({top:intro.getBoundingClientRect().top+scrollY-30,behavior:'instant'});return {width:innerWidth,overflow:document.documentElement.scrollWidth>innerWidth,cards:[...document.querySelectorAll('.feature-benefit')].map(el=>({width:el.clientWidth,overflow:el.scrollWidth>el.clientWidth,color:getComputedStyle(el).color})),font:getComputedStyle(intro.querySelector('em')).fontFamily,errors:window.__auditErrors};})()